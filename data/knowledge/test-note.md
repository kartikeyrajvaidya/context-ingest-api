# Test note

This is a small seed document used to exercise the local-file branch of
`scripts/ingest_all.py`. It verifies that markdown entries in
`data/sources.json` are read from disk, cleaned, chunked, embedded, and
persisted under their synthetic `knowledge://` identifier.

## Why it exists

The content is arbitrary — only its presence matters for end-to-end smoke
testing. Feel free to delete this file once you add real local knowledge
entries to the manifest.
